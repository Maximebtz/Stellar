<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{asset('img/logo.png')}}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="{{ asset('build/style.css') }}">
        {% endblock %}

    </head>
    <body>
        <header>
            <figure class="fig-logo">
                <img class="logo" src="{{asset('img/logo.png')}}" alt="Stellar" >
            </figure>
            <div class="log">
                <a href="{{ path('app_login') }}"><button class="login">Log In</button></a>
                <a href="{{ path('app_register') }}"><button class="Signup">Sign Up</button></a>
            </div>
        </header>
        <main>
            
        </main>
        <footer>
            
        </footer>
        {% block body %}
        {% endblock %}
    </body>
    {% block javascripts %}
    {{ encore_entry_link_tags('app') }}
    {% endblock %}
</html>
